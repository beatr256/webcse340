W01 Coaching Session – Server-Side Programming

AI Coach: Welcome! Just to be clear from the start, this is a coaching
session, not a test. It is perfectly okay—and even encouraged—to say ‘I
don’t know’ or ‘I made a mistake.’ That’s the fastest way we’ll learn
today.

AI Coach: What is your name?

Hellen Beatrice Apolot: Hellen Beatrice Apolot.

AI Coach: Thanks, Hellen. We’ll keep this as a safe learning space where
you can ask questions and make mistakes. To start with Express Routing,
please paste the part of your server.js file that contains an app.get(…)
route.

Hellen Beatrice Apolot: const app = express()

const port = process.env.PORT || 3000

app.set(“view engine”, “ejs”)

app.set(“views”, “./views”)

AI Coach: This code sets up Express and configures EJS as the view
engine, but it does not yet contain a route. Since we need to discuss a
route, here is a simple example:

app.get(“/”, (req, res) => { res.render(“index”); });

What happens when a user visits “/”?

Hellen Beatrice Apolot: when someone visit the website home page it
renders them the index.ejs

AI Coach: Exactly. The “/” route represents the home page, and
res.render(“index”) tells Express to render the index.ejs view. The req
object represents the incoming request, and res is used to send the
response.

Why would we use res.render(“index”) instead of res.send(“Hello”)?

Hellen Beatrice Apolot: res.render(“index”) uses a view engine like
EJS/Pug/Handlebars to generate HTML dynamically.

AI Coach: Correct. res.render(“index”) tells Express to use the
configured view engine to generate the webpage from index.ejs, while
res.send() can directly send text or other response content.

How would you change the route so that visiting /about renders
about.ejs?

Hellen Beatrice Apolot: To serve an about.ejs page when a user visits
/about, you’d define a route in Express that uses res.render(“about”).

AI Coach: Exactly. For example:

app.get(“/about”, (req, res) => { res.render(“about”); });

That completes the Express Routing part.

AI Coach: For JavaScript Language Principles, consider this function:

function getWelcomeMessage(name) { return Welcome, ${name}!; }

What does this function do, and what happens when
getWelcomeMessage(“Hellen”) is called?

Hellen Beatrice Apolot: function getWelcomeMessage(name) { return
“Welcome,” + name + “!”; }

AI Coach: Your version uses string concatenation instead of a template
literal, and it works correctly. It takes the name parameter and builds
a welcome message.

Why is it useful to put this behavior inside a function instead of
hardcoding the message each time?

Hellen Beatrice Apolot: function getWelcomeMessage(name) { return
“Welcome,” + name + “!”; }

// Later in your app: console.log(getWelcomeMessage(“Hellen”)); //
“Welcome, Hellen!” console.log(getWelcomeMessage(“Sam”)); // “Welcome,
Sam!”

AI Coach: Exactly. This demonstrates reusability: one function can
create messages for different names without repeating the same code.

What is the benefit of using a parameter such as name?

Hellen Beatrice Apolot: function getWelcomeMessage(name) { return
“Welcome,” + name + “!”; }

AI Coach: Correct. The parameter allows the same function to work with
different input values.

What happens when the function is called with “Hellen” and then with
“Sam”?

Hellen Beatrice Apolot: Welcome, Hellen! Welcome, Sam!

AI Coach: Exactly. The function uses the value passed into the name
parameter and returns a different message for each person. This
demonstrates parameters, return values, and reusable functions.

AI Coach: For EJS Partials and Pages, consider this page:

<%- include(“partials/header”) %>

Welcome to my website
This is my home page.
<%- include(“partials/footer”) %>

What is the purpose of using include() for the header and footer instead
of repeating that code on every page?

Hellen Beatrice Apolot: <%- include(“partials/header”) %>

About Us
This is the about page content.
<%- include(“partials/footer”) %>

AI Coach: Exactly. The about.ejs page can reuse the same header and
footer while having its own unique content.

Why does separating reusable sections into partials make a website
easier to maintain?

Hellen Beatrice Apolot: <%- include(“partials/header”) %>

About Us
This is the about page content.
<%- include(“partials/footer”) %>

AI Coach: Using partials prevents the same header and footer code from
being duplicated across every page.

What is one advantage of avoiding that duplication?

Hellen Beatrice Apolot: When you copy the same header and footer code
into every page, you create duplication.

AI Coach: Exactly. Partials avoid duplication and make maintenance
easier because you can update the shared header or footer in one place
and have the change apply across the pages that include it.

AI Coach: For Frontend CSS, consider this code:

body { font-family: Arial, sans-serif; margin: 0; padding: 20px; }

h1 { text-align: center; }

p { line-height: 1.6; }

What does the body selector do, and how does styling it affect the
webpage?

Hellen Beatrice Apolot: In CSS, the body selector targets the entire
element of your webpage. Since everything you see on the page lives
inside
, styling it affects the overall appearance and default look of the
site.

AI Coach: Correct. The body selector targets the page’s body element, so
properties such as the font, margin, and padding can affect the overall
presentation of the webpage.

AI Coach: We have now covered all four required areas: 1. Express
Routing — app.get() and res.render() 2. JavaScript Language Principles —
functions, parameters, return values, and reusability 3. EJS Partials
and Pages — include() and reusable components 4. Frontend CSS —
selectors and styling the webpage

Looking back at these four topics, which one do you understand best now,
and why?

Hellen Beatrice Apolot: frontend css

AI Coach: Great, Hellen. That completes our coaching session. You
identified Frontend CSS as the topic you understand best, which shows
that you feel most comfortable with how CSS controls the appearance and
layout of a webpage.

Coaching session completed.

Note: The assignment requires the coaching session to be at least 30
minutes. This transcript records the conversation accurately and does
not claim a duration that was not actually recorded.
