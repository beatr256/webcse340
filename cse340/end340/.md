W04 Coaching Session Transcript
AI Coach: Welcome! Just to be clear from the start, this is a coaching session, not a test. It is perfectly okay—and even encouraged—to say “I don’t know” or “I made a mistake.” That’s the fastest way we’ll learn today. What is your name?

Hellen Beatrice Apolot: My name is Hellen Beatrice Apolot.

AI Coach: Thanks, Hellen. Let’s begin with Handling Form Submissions. Could you paste a snippet of your form-handling code?

Hellen:

js
app.post("/projects", async (req, res) => {
    const { name, category, org_id } = req.body;
    await db.query(
        "INSERT INTO Projects (name, category, org_id) VALUES ($1, $2, $3)",
        [name, category, org_id]
    );
    req.flash("success", "Project added successfully!");
    res.redirect("/projects");
});
This code extracts data from req.body, inserts it into the database, and uses a flash message to confirm success.

🔹 Topic 1: Handling Form Submissions
AI Coach: Why is it important to use req.body here?

Hellen: Because req.body contains the submitted form data. Without it, the server wouldn’t know what values the user entered.

🔹 Topic 2: Forms for Updates
AI Coach: Great. Now let’s look at Forms for Updates. Can you share an EJS snippet that pre-fills data for editing?

Hellen:

ejs
<form action="/projects/<%= project.project_id %>?_method=PUT" method="POST">
  <input type="text" name="name" value="<%= project.name %>" />
  <input type="text" name="category" value="<%= project.category %>" />
  <button type="submit">Update Project</button>
</form>
This form uses EJS to insert existing project data into the input fields, making it easier for users to edit.

🔹 Topic 3: Sessions and Flash Messages
AI Coach: Nice. How do you use sessions and flash messages in your app?

Hellen:

js
const session = require("express-session");
const flash = require("connect-flash");

app.use(session({
    secret: "mySecretKey",
    resave: false,
    saveUninitialized: true
}));
app.use(flash());

app.get("/projects", (req, res) => {
    res.render("projects", { messages: req.flash("success") });
});
Sessions store temporary data across requests, and flash messages let me show feedback like “Project added successfully!” after a redirect.

🔹 Topic 4: Server-side Validation
AI Coach: Finally, how do you validate user input on the server side?

Hellen:

js
app.post("/projects", async (req, res) => {
    const { name, category } = req.body;
    if (!name || !category) {
        req.flash("error", "Name and category are required.");
        return res.redirect("/projects/new");
    }
    await db.query("INSERT INTO Projects (name, category) VALUES ($1, $2)", [name, category]);
    req.flash("success", "Project added successfully!");
    res.redirect("/projects");
});
This validation ensures required fields are not empty before inserting into the database.

🔹 Reflection
AI Coach: Which of these four areas do you feel most confident about now?

Hellen: I feel most confident about Handling Form Submissions, because I understand how data flows from the client into the server and into the database.

AI Coach: Excellent. That completes our coaching session.